# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a new, empty project. The repository is located at `/Users/qw/Desktop/qw/codes/claude/drink/`.

## Getting Started

- No build system or dependencies are configured yet.
- Initialize the project as needed (e.g., `npm init`, `cargo init`, `pipenv install`, etc.) depending on the language and framework chosen.
- After adding a build system, update this file with relevant commands for building, testing, and linting.

## Conventions

- Follow the language/framework standard project structure.
- Keep the root directory clean — place source code in appropriate subdirectories (e.g., `src/`, `lib/`, `app/`).
